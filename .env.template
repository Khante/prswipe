# GitHub OAuth App credentials - REQUIRED
# To create: Go to GitHub Settings > Developer settings > OAuth Apps > New OAuth App
# 1. Application name: PRswipe
# 2. Homepage URL: http://localhost:3000
# 3. Authorization callback URL: http://localhost:3000/auth/callback
# 4. Generate a new client secret in the app settings
GITHUB_CLIENT_ID=your_client_id_here
GITHUB_CLIENT_SECRET=your_client_secret_here

# Secret key for signing session cookies - REQUIRED
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here
